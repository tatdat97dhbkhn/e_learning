<div class="container margin-top-100">
  <%= form_for :questionlog, method: :patch do |f| %>
    <% if @lession_log.pass.nil? %>
    <% (0...@question_logs.count).each do |i| %>
      <div>
        <b><%= i+1 %>. <%= @questions[i].content %></b>
        <% @answers[i].each do |ans| %>
          <% next if ans.id == Settings.question.answer_default %>
          <li class="answer">
            <%= f.radio_button @question_logs[i].id, ans.id %>
            <%= f.label "#{@question_logs[i].id}_#{ans.id}",
              ans.content %>
          </li>
        <% end %>
      </div>
    <% end %>
    <%= f.submit t(".finish"), class: "btn btn-primary" %>
    <% else %>
      <%= render "history" %>
    <% end %>
  <% end %>
</div>
